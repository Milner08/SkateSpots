<%= form_for(current_user.likes.build(user_id: current_user.id), remote: true) do |f| %>
	<%= hidden_field_tag :spot_id, @spot.id %>
  <%= f.submit "Like", class: "btn btn-primary btn-medium floatright" %>
<% end %>