<h1 class="page-header">Spot <small><%= @photo.title %> for <%= @photo.spot.name %></small></h1>
<div class="row">
	<div class="span12">
		<div class="span7">
			<%= image_tag @photo.picture.url(:large), :class => "photo"%>	
		</div>	
		<div class="span4">
			<h3><%= @photo.title %></h3>
			<p>Description:
				<%= @photo.description %></p>
			<p>By: <%= @photo.user.name %></p>
			<% if @photo.user = current_user || current_user.admin? %>
				<%= link_to "Edit", edit_photo_path(@photo), :class => "btn btn-info" %>
				<%= link_to "Delete", @photo, method: :delete, confirm: "You sure?", class: "btn btn-danger" %>
			<% end %>
		</div>
	</div>
</div>